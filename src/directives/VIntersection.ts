export default {
  // mounted(elem: HTMLElement, binding: any) {
  //   // console.log(elem, binding);
  //   const options = {
  //     rootMargin: "0px",
  //     threshold: 1.0,
  //   };
  //   const callback = (entries: any, observer: any) => {
  //     if (entries[0].isIntersecting) {
  //       binding.value();
  //     }
  //   }; // функция, что сработает при пересечении елемента
  //   const observer = new IntersectionObserver(callback, options); //объект, созд. на основе коллбэка
  //   observer.observe(elem);
  // },
  // name: "intersection",
};
